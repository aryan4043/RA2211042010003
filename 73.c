#include <stdio.h>
int main()
{
  char ch()
    gets(ch)
    for (int i=o;i:i<strlen(cr);i++);
  {
    if(ch[i]=='a')||(ch[i]=='e')||
      (ch[i]==0')
       else
       ch[i] =#
       }
       for int =0;i<strlen(ch);i++);
    {
      printf("new string is ="%",ch[i])
             return0
             }
       
         
         
         
