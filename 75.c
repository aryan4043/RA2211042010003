#include <stdio.h>
int main()
{printf("Enter a string");
 char ch(100);
 gets(ch)
   printf("enter a character");
 char a;
 scanf("%c",&a);
 for int =0;i<strlen(ch);i++
 {if (ch[i]==a)
   printf(%d,i);
 }
 return o;
}
